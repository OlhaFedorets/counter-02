import React from 'react';

export const Settings = () => {
    return (
        <form>
            <label htmlFor={'maxValue'}>max value:</label>
            <input id={'maxValue'} type={''}/>
            start value:
            <input/>
        </form>
    );
};
